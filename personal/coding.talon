
-
code const:
    insert("const ")

code bool:
    insert("bool ")

code char:
    insert("char ")

spin paren:
    insert(")")

spin brace:
    insert("}")

spin square:
    insert("]")

spin angle:
    insert(">")
